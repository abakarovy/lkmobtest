name: lkmobileapp
description: Личный кабинет абонента Fly-tech

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/sstudio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 2.0.1+8

environment:
  sdk: ">=2.17.6 <3.0.0"
# ">=2.17.6 <3.0.0" это версия с нулевой безопасностью
# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  oauth2:
  


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
#  fluttertoast: ^8.2.4
  webview_flutter: ^4.4.4
  getwidget: ^4.0.0
  bloc: ^8.1.0
  intl: ^0.19.0
  date_format:
  http: ^1.2.0
  js: ^0.7.1
  provider: ^6.0.3
  html:
  flutter_bloc: ^8.1.1
  #flutter_no_internet_widget:
  http_auth: ^1.0.1
  shared_preferences: ^2.0.15
  dio:
  dropdown_button2: ^2.3.9
  firebase_messaging:
  firebase_core: ^3.3.0
  flutter_local_notifications:
  universal_platform:
  webview_flutter_web: ^0.2.3+2
  url_launcher:

dev_dependencies:
  flutter_launcher_icons: "^0.14.1"
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints:

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  
  assets:
    - assets/images/login.png
    - assets/images/register.png
    - assets/images/sberbank.png
    - assets/images/Fly-tech Logo_V.png
    - assets/images/tinkoff.png
    - assets/sbp.png
    - assets/icons/absolut.png
    - assets/icons/akbars.png
    - assets/icons/alfabank.png
    - assets/icons/artsofte.png
    - assets/icons/avangard.png
    - assets/icons/bankuralsib.png
    - assets/icons/bbr.png
    - assets/icons/blanc.png
    - assets/icons/brs.png
    - assets/icons/bspb.png
    - assets/icons/centrinvest.png
    - assets/icons/elplat.png
    - assets/icons/expobank.png
    - assets/icons/factura.png
    - assets/icons/gazprombank.png
    - assets/icons/gebank.png
    - assets/icons/hakasskii.png
    - assets/icons/hlynov.png
    - assets/icons/homecredit.png
    - assets/icons/kebr.png
    - assets/icons/kubankredit.png
    - assets/icons/letobank.png
    - assets/icons/levoberezhny.png
    - assets/icons/mkb.png
    - assets/icons/mosoblbank.png
    - assets/icons/mts.png
    - assets/icons/novikom.png
    - assets/icons/openbank.png
    - assets/icons/otpbank.png
    - assets/icons/pcb.png
    - assets/icons/pskb.png
    - assets/icons/qivi.png
    - assets/icons/raiffeisen.png
    - assets/icons/rc.png
    - assets/icons/rncb.png
    - assets/icons/rosbank.png
    - assets/icons/roscap.png
    - assets/icons/rostfb.png
    - assets/icons/rshb.png
    - assets/icons/sber.png
    - assets/icons/sinara.png
    - assets/icons/smp.png
    - assets/icons/solidarnost.png
    - assets/icons/sovcomcard.png
    - assets/icons/tinkoff.png
    - assets/icons/tkbbank.png
    - assets/icons/ucb.png
    - assets/icons/vbrr.png
    - assets/icons/vtb.png
    - assets/icons/ybrir.png
    - assets/icons/zenit.png

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
